# ============================================================
# augment-lite-mcp 環境變數配置
# ============================================================
# 默认使用原厂 Anthropic 格式，只需设置 API Keys

# 資料目錄
AUGMENT_DB_DIR=./data

# ============================================================
# GLM-4.7 原厂 (必需)
# ============================================================
GLM_API_KEY=your-glm-api-key

# ============================================================
# MiniMax-M2.1 原厂 (必需)
# ============================================================
MINIMAX_API_KEY=your-minimax-api-key

# ============================================================
# Requesty.ai (可选 - 用于 long-context 路由)
# ============================================================
REQUESTY_API_KEY=sk-your-requesty-api-key

# ============================================================
# OpenRouter (用於 Embedding - Qwen3-Embedding-4B)
# ============================================================
# 從 https://openrouter.ai/keys 獲取
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key

# ============================================================
# 本地代理模式 (可选 - claude-code-proxy)
# ============================================================
# 如需使用本地代理而非原厂，设置以下变量并修改 config/models.yaml 的 routes

# GLM 本地代理 (端口 8082)
# GLM_LOCAL_BASE_URL=http://127.0.0.1:8082/v1
# GLM_LOCAL_API_KEY=dummy
# GLM_LOCAL_MODEL_ID=GLM-4.7

# MiniMax 本地代理 (端口 8083)
# MINIMAX_LOCAL_BASE_URL=http://127.0.0.1:8083/v1
# MINIMAX_LOCAL_API_KEY=dummy
# MINIMAX_LOCAL_MODEL_ID=MiniMax-M2.1

# Gemini 本地代理 (端口 8084) - 已禁用
# GEMINI_LOCAL_BASE_URL=http://127.0.0.1:8084/v1
# GEMINI_LOCAL_API_KEY=dummy
# GEMINI_LOCAL_MODEL_ID=gemini-2.5-flash

# ============================================================
# 使用說明
# ============================================================
# 1. 複製: cp .env.example .env
# 2. 填入必需的 API keys:
#    - GLM_API_KEY (從 z.ai 獲取)
#    - MINIMAX_API_KEY (從 minimax.io 獲取)
#    - OPENROUTER_API_KEY (從 openrouter.ai/keys 獲取) - 用於 Embedding
# 3. 可選: REQUESTY_API_KEY (從 requesty.ai 獲取)
# 4. 啟動服務即可
#
# Provider 配置:
#   glm-4.7:      原廠 https://api.z.ai/api/coding/paas/v4
#   minimax-m2.1: 原廠 https://api.minimax.io/v1
#   embedding:    OpenRouter qwen/qwen3-embedding-4b
#   glm-local:    本地代理 (需配置 GLM_LOCAL_* 變數)
#   minimax-local: 本地代理 (需配置 MINIMAX_LOCAL_* 變數)
#
# Embedding 配置 (config/models.yaml):
#   - 預設使用 OpenRouter API (qwen/qwen3-embedding-4b, 1024 維)
#   - 若 OPENROUTER_API_KEY 未設定，自動 fallback 到本地模型
#   - 本地 fallback: sentence-transformers/all-MiniLM-L6-v2 (384 維)
# ============================================================
